[project]
# this is an early draft do not use
name = "PyQtByExample"
version = "0.1.0"
description = "Example code for Python and Qt ( 5 6 and PySide"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "qtpy",
    "unidecode>=1.3.0",
    "wat-inspector",
    "pyqtgraph",
    "matplotlib"
    "QScintilla",
]

